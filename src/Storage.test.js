import Storage from './Storage';

describe('test block',() => {
	it('fails',() => {
		expect(false).toBeFalsy();
	});

	it('succeed',() => {
		expect(true).toBeTruthy();
	});

	//it('wtf', () => {
	//	expect(qwe.tst()).toBeFalsy();
	//})
});